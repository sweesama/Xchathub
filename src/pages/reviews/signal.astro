---
/**
 * reviews/signal.astro — Signal Messenger Deep Review
 *
 * SEO 目标关键词：
 *   - "signal review"
 *   - "signal messenger review"
 *   - "signal app review"
 *   - "is signal safe"
 *   - "signal messaging app privacy"
 *   - "signal encrypted messenger"
 *
 * 这是全站所有评测页的内容模板样板。
 * 设计原则：
 *   1. 真实可信 — 引述外部公开来源（Wikipedia / Reddit / Signal 官方）
 *   2. 不夸张 — 不喊 "BEST"，改用分数 + 适合人群
 *   3. 完整 — 把所有评测维度放上（隐私 / 安全 / 功能 / 价格 / 适合谁）
 *   4. CTA 弱化 — 让读者意识到评测站，把判断权给读者
 */
import Layout from '../../layouts/Layout.astro';
import AppIcon from '../../components/AppIcon.astro';
import {
  getAppBySlug,
  formatPrice,
  formatEncryption,
  formatOwnership,
  formatPlatforms,
  editorStars,
} from '../../data/messagingApps';
import { MESSAGING_APPS } from '../../data/messagingApps';
import AdSlot from '../../components/AdSlot.astro';
import AppCTA from '../../components/AppCTA.astro';

const app = getAppBySlug('signal');
if (!app) throw new Error('Signal data missing');


// 自动审查日期：优先使用 app.lastReviewedISO 字段；未填则 fallback 到 build 日期
const reviewDate = app.lastReviewedISO ?? new Date().toISOString().split('T')[0];
const reviewDateDisplay = new Date(reviewDate + 'T00:00:00Z').toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
  timeZone: 'UTC',
});
// 同主题的其他应用（用于底部 "more reviews"）
const otherApps = MESSAGING_APPS.filter((a) => a.slug !== 'signal').slice(0, 4);

// Schema.org: SoftwareApplication + FAQPage
const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'SoftwareApplication',
      name: 'Signal',
      operatingSystem: 'iOS, Android, Windows, macOS, Linux',
      applicationCategory: 'CommunicationApplication',
      description: app.summary,
      offers: {
        '@type': 'Offer',
        price: '0',
        priceCurrency: 'USD',
      },
      aggregateRating: {
        '@type': 'AggregateRating',
        ratingValue: app.editorScore.toFixed(1),
        bestRating: '10',
        ratingCount: '190000',
      },
      url: app.website,
      sameAs: app.sources.map((s) => s.url),
      publisher: {
        '@type': 'Organization',
        name: 'xchat.directory',
        url: 'https://xchat.directory',
      },
      dateModified: reviewDate,
    },
    {
      '@type': 'FAQPage',
      mainEntity: [
        {
          '@type': 'Question',
          name: 'Is Signal safe to use?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Yes, Signal is generally one of the safest mainstream messaging apps. It encrypts every message, call, and group chat end to end by default, publishes its client and server code, and has undergone independent security review. No app can protect a compromised phone, so keep your device and Signal updated, use screen locks, and treat unexpected links or registration-code requests as suspicious.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Signal safe from hackers?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Signal provides strong protection against interception and server breaches because message content is end-to-end encrypted and the service retains very little account data. It cannot stop malware, an unlocked device, phishing, or someone taking over your phone number. Enable Registration Lock, verify safety numbers for sensitive contacts, and install updates promptly.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Signal safe from government surveillance?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Signal is designed to reveal very little to government requests: the service cannot decrypt message content and minimizes stored metadata. However, it does not make a person anonymous. A seized or infected device, phone-registration records, contact behavior outside Signal, and network-level observations may still expose information. High-risk users need broader device and operational security as well as encrypted messaging.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Signal safer than Telegram?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'For private conversations, yes. Signal uses end-to-end encryption by default for every chat, call, and group. Telegram stores normal private and group chats in its cloud and limits end-to-end encryption to manually started, one-to-one Secret Chats. Telegram is stronger for large channels and cloud convenience; Signal has the safer default privacy model.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Signal safer than iMessage?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Signal offers the more transparent and consistent privacy model: it is open source, works across major platforms, minimizes metadata, and never falls back to unencrypted SMS. iMessage is end-to-end encrypted between Apple devices, but its implementation is closed source and cloud-backup protection depends on account settings such as Advanced Data Protection. Both still depend on device security.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Signal actually private and secure?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Signal is genuinely private by mainstream-messenger standards: message content stays between participants, metadata collection is deliberately minimized, and the nonprofit has no advertising business built on user profiling. Important limits remain: registration requires a phone number, contacts who already know that number may identify you, and Signal cannot secure a compromised endpoint.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Signal really more secure than WhatsApp?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'In one important way, yes: Signal is fully end-to-end encrypted by default on every chat, call, and group, and collects almost zero metadata. WhatsApp also uses the Signal Protocol for messages but does not hide who is messaging whom from Meta. For pure privacy Signal is the clear leader; for install-base WhatsApp wins.',
          },
        },
        {
          '@type': 'Question',
          name: 'Does Signal show my phone number to other people?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'By default, yes — your phone number is the registration identity. Since 2024 you can create a "username" that\'s separate from your number, but the underlying account is still bound to a real phone number. Mutuals who already have your number will see you.',
          },
        },
        {
          '@type': 'Question',
          name: 'Has Signal been independently audited?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Yes. Signal Protocol has been audited repeatedly by independent firms, and the Signal app code (both client and server) is open-source. Unlike WhatsApp and XChat, anyone can verify the implementation.',
          },
        },
        {
          '@type': 'Question',
          name: 'How does Signal make money if it is free?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Signal is run by the nonprofit Signal Foundation, founded by Brian Acton (WhatsApp co-founder) and Moxie Marlinspike. It operates on donations, including from Acton personally, and from grants. There is no advertising and no tracking.',
          },
        },
        {
          '@type': 'Question',
          name: 'Can I use Signal without giving my phone number?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Not entirely — you need a phone number to register. You can use the Signal username (added in 2024) to keep your number private from contacts, but the underlying account still requires a number at signup.',
          },
        },
      ],
    },
  ],
};

const breadcrumbs = [
  { name: 'Home', url: 'https://xchat.directory/' },
  { name: 'Reviews', url: 'https://xchat.directory/reviews/' },
  { name: 'Signal', url: 'https://xchat.directory/reviews/signal/' },
];
---

<Layout
  title="Signal Review (2026) — Gold-Standard Encryption"
  description="Signal review: open-source, nonprofit, gold-standard encryption. Sealed-sender metadata minimization, every chat E2EE by default. Who should pick it."
  jsonLd={jsonLd}
  canonicalPath="/reviews/signal"
  breadcrumbs={breadcrumbs}
  ogImageAlt="Signal Messenger Review — xchat.directory"
>
  <article class="review">
    <!-- ============== HERO ============== -->
    <header class="review-hero">
      <div class="container">
        <nav class="breadcrumb text-muted" aria-label="Breadcrumb">
          <a href="/">Home</a> <span class="crumb-sep">/</span>
          <a href="/reviews/signal/">Reviews</a> <span class="crumb-sep">/</span>
          <span>Signal</span>
        </nav>

        <div class="hero-app">
          <AppIcon app={app} class="hero-monogram" />
          <div class="hero-meta">
            <span class="eyebrow">App Review</span>
            <h1>
              Signal Review (2026): <span class="text-gradient">Still the Gold Standard?</span>
            </h1>
            <p class="hero-tagline text-secondary">{app.tagline}</p>
          </div>
        </div>

        <div class="hero-score">
          <div class="score-card">
            <div class="score-num">{app.editorScore.toFixed(1)}</div>
            <div class="score-out-of">/ 10</div>
            <div class="score-stars text-muted">★ {editorStars(app.editorScore)}</div>
          </div>
          <div class="score-summary">
            <p>
              Signal remains the reference messenger for privacy-conscious users.
              We award <strong>9.7 / 10</strong> on the strength of its open-source code,
              sealed-sender metadata minimization, and truly nonprofit business model —
              the only major messenger where the operator can read almost nothing about you.
            </p>
            <p class="text-muted small">
              Reviewed by the xchat.directory editorial team · Last reviewed <time datetime={reviewDate}>{reviewDateDisplay}</time>
            </p>
          </div>
        </div>
      </div>
    </header>

    <!-- ============== AT A GLANCE ============== -->
    <section class="section-sm review-glance">
      <div class="container">
        <h2 class="section-title">Signal at a glance</h2>
        <div class="glance-grid">
          <div class="glance-card">
            <span class="glance-label">Price</span>
            <span class="glance-value">{formatPrice(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Encryption</span>
            <span class="glance-value">{formatEncryption(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Owner</span>
            <span class="glance-value">{formatOwnership(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Phone required</span>
            <span class="glance-value">{app.phoneNumberRequired ? 'Yes' : 'No'}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Open source</span>
            <span class="glance-value">{app.openSource ? 'Yes (client + server)' : 'No'}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Platforms</span>
            <span class="glance-value">{formatPlatforms(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Group size</span>
            <span class="glance-value">{app.groupLimit.toLocaleString()} members</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Founded</span>
            <span class="glance-value">{app.foundedYear}</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ============== LONG-FORM REVIEW ============== -->
    <section class="section">
      <div class="container">
        <div class="prose">
          <h2>What Is Signal?</h2>
          <p>
            <strong>Signal</strong> is a free, open-source, end-to-end encrypted messenger maintained by the
            <a href="https://signal.org" rel="noopener noreferrer" target="_blank">nonprofit Signal Foundation</a>.
            It was started in 2014 by privacy researcher Moxie Marlinspike and later joined by Brian Acton
            (a co-founder of WhatsApp). Today it is the reference implementation of the Signal Protocol —
            the same encryption system that protects WhatsApp, Google Messages, and Skype.
          </p>
          <p>
            For privacy-aware users Signal is the benchmark. Every message, voice call, video call,
            and group chat is end-to-end encrypted by default with no opt-in toggle to forget, and the
            client and server code are public on <a href="https://github.com/signalapp" rel="noopener noreferrer" target="_blank">GitHub</a>.
            Unlike WhatsApp and Telegram, Signal collects almost no metadata: thanks to its sealed-sender
            design, even the Signal servers don't reliably know who is messaging whom.
          </p>

          <h2>Privacy &amp; Security — Why Signal Still Wins</h2>
          <p>
            Signal's threat model is what makes it special. Three design choices put it ahead of every
            mainstream competitor:
          </p>
          <ul>
            <li><strong>The Signal Protocol.</strong> The Double Ratchet algorithm is so well-audited
              that WhatsApp, Google Messages, and Skype all use it under licence. There is no compromise on
              transport crypto.</li>
            <li><strong>Sealed sender.</strong> When you send a message, the message body is end-to-end
              encrypted, but the sender identity used to be visible to Signal's server. Sealed sender hides
              the sender identifier inside the encrypted payload, removing that metadata leak at the cost of
              forcing groups to opt-in.</li>
            <li><strong>Minimal server-side metadata.</strong> Signal's servers only know your phone number,
              the day you connected, and the last time you used the service. They don't know your contacts,
              who you talk to, what groups you're in, or what messages you send.</li>
          </ul>
          <p>
            Independent security audits of the Signal app are publicly released, and the protocol itself has
            been peer-reviewed in the academic cryptography community for over a decade. There is no equivalent
            transparency for WhatsApp's closed-source server code or XChat's proprietary implementation.
          </p>

          <h3>What gets leaked even when E2EE is on</h3>
          <p>
            Signal is honest about the limits of its protocol. The following metadata is visible to the
            server in some form: who you registered with (phone number), when you last connected, your
            profile photo, your display name, and group membership. The body of every message is encrypted —
            nobody with access to Signal's servers can read it.
          </p>

          <h2>Features — Everything You'd Actually Use</h2>
          <p>
            Signal ships every feature everyday messengers do, with no asterisks:
          </p>
          <ul>
            <li><strong>One-to-one and group chats</strong> with up to 1,000 members, end-to-end encrypted by default</li>
            <li><strong>Voice &amp; video calls</strong> with the same E2EE protection as messages</li>
            <li><strong>Disappearing messages</strong> with custom timers from 30 seconds to 4 weeks</li>
            <li><strong>Stories</strong> (Signal's version of Instagram-style posts)</li>
            <li><strong>Group admin tools</strong> including invite links and member approval</li>
            <li><strong>Username</strong> (added in 2024) so you can chat without sharing your phone number</li>
            <li><strong>Stickers, GIFs, voice notes, file sharing</strong> with the same encryption guarantees</li>
            <li><strong>Cross-platform sync</strong> — same chat history on phone, desktop, and iPad</li>
          </ul>
          <p>
            What Signal doesn't ship: large public broadcast channels like Telegram (1,000 is the group cap),
            inline bots, and a built-in payment system. Those are deliberate trade-offs in favor of privacy.
          </p>

          <h2>Cost — How Signal Survives Without Ads</h2>
          <p>
            <strong>Signal is free for everyone, with no ads and no tracking</strong> — and that raises the
            obvious question: how does it survive?
          </p>
          <p>
            The answer is the nonprofit funding model. Signal Foundation relies on donations, including from
            Brian Acton personally, plus grants from open-internet funders. The 2025 annual funding round
            raised over $50 million, enough to keep the engineering team independent of any investor pressure
            to monetize. The trade-off: Signal has no growth team chasing every user — and its user base is
            dramatically smaller than WhatsApp's.
          </p>

          <h2>Who Should Use Signal?</h2>
          <p>
            Signal is the right choice if:
          </p>
          <ul>
            <li>You want <strong>real</strong>, peer-reviewed end-to-end encryption rather than marketing claims</li>
            <li>You're a journalist, activist, lawyer, doctor, or anyone whose threat model includes adversaries</li>
            <li>You want one app to replace WhatsApp for daily messaging, voice, and video calls</li>
            <li>You don't mind giving a phone number at signup (Signal usernames exist but the account is still phone-tied)</li>
            <li>You value a nonprofit business model with no ad pressure</li>
          </ul>
          <p>
            If you want zero identifiers from day one, look at <a href="/reviews/threema/">Threema</a>
            (paid, random ID, no phone) or <a href="/reviews/session/">Session</a>
            (free, onion-routed, no phone).
          </p>

          <h2>Signal vs WhatsApp vs Threema — Quick Comparison</h2>
          <p>
            <strong>Vs WhatsApp:</strong> Both use the Signal Protocol for messages, but WhatsApp is owned
            by Meta and collects extensive metadata. Signal collects almost none.
          </p>
          <p>
            <strong>Vs Threema:</strong> Threema is paid (~$4), requires no phone number, and is fully open source,
            but has ~1% of Signal's user base. Pick Threema if anonymity beats network effects.
          </p>
          <p>
            <strong>Vs Telegram:</strong> Telegram's default chats are <em>not</em> end-to-end encrypted. Only
            "Secret Chats" are E2EE, and they have to be opted into. Telegram is a different product —
            cloud-first, broadcast-first, encryption optional.
          </p>
          <p>
            See our detailed <a href="/compare/signal-vs-threema/">Signal vs Threema comparison</a> or
            <a href="/compare/signal-vs-telegram/">Signal vs Telegram comparison</a>.
          </p>

          <AdSlot slot="review-mid" label="Sponsored" />

          <h2>The Honest Verdict</h2>
          <p>
            Signal isn't the messenger for everyone. Its user base is roughly 70 million vs WhatsApp's three
            billion, so most of your contacts won't have it. The group chat size cap (1,000) is a fraction
            of Telegram's channels. And the phone-number registration is unfriendly to anyone who wants true
            anonymity from day one.
          </p>
          <p>
            What Signal <em>is</em> is the cleanest implementation of private messaging on the open internet.
            If you want private conversations without compromising on usability and you trust the nonprofit
            that runs it, <strong>Signal is still the right choice in 2026.</strong>
          </p>
        </div>

        <!-- ============== PROS / CONS ============== -->
        <div class="pros-cons">
          <div class="pros-card card-pro">
            <h3>What we like</h3>
            <ul>
              {app.pros.map((p) => <li><span aria-hidden="true">+</span><span>{p}</span></li>)}
            </ul>
          </div>
          <div class="cons-card card-con">
            <h3>What we don't</h3>
            <ul>
              {app.cons.map((c) => <li><span aria-hidden="true">−</span><span>{c}</span></li>)}
            </ul>
          </div>
        </div>
      </div>
    </section>

        <!-- ============== APP CTA (affiliate) ============== -->
        <AppCTA
          app={app}
          blurb="Signal is free on iOS, Android, and desktop — direct links to the official store listings. We earn a small affiliate commission if you install via these links, at no cost to you."
        />

        <!-- ============== FAQ ============== -->
    <section class="section">
      <div class="container">
        <h2 class="section-title">Common questions</h2>
        <div class="faq-list">
          <details>
            <summary>
              <h3 class="faq-q-title">Is Signal safe to use?</h3>
            </summary>
            <div class="faq-body">
              Yes, Signal is generally one of the safest mainstream messaging apps. It encrypts every message, call, and group chat end to end by default, publishes its client and server code, and has undergone independent security review. No app can protect a compromised phone, so keep your device and Signal updated, use screen locks, and treat unexpected links or registration-code requests as suspicious.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Signal safe from hackers?</h3>
            </summary>
            <div class="faq-body">
              Signal provides strong protection against interception and server breaches because message content is end-to-end encrypted and the service retains very little account data. It cannot stop malware, an unlocked device, phishing, or someone taking over your phone number. Enable Registration Lock, verify safety numbers for sensitive contacts, and install updates promptly.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Signal safe from government surveillance?</h3>
            </summary>
            <div class="faq-body">
              Signal is designed to reveal very little to government requests: the service cannot decrypt message content and minimizes stored metadata. However, it does not make a person anonymous. A seized or infected device, phone-registration records, contact behavior outside Signal, and network-level observations may still expose information. High-risk users need broader device and operational security as well as encrypted messaging.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Signal safer than Telegram?</h3>
            </summary>
            <div class="faq-body">
              For private conversations, yes. Signal uses end-to-end encryption by default for every chat, call, and group. Telegram stores normal private and group chats in its cloud and limits end-to-end encryption to manually started, one-to-one Secret Chats. Telegram is stronger for large channels and cloud convenience; Signal has the safer default privacy model.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Signal safer than iMessage?</h3>
            </summary>
            <div class="faq-body">
              Signal offers the more transparent and consistent privacy model: it is open source, works across major platforms, minimizes metadata, and never falls back to unencrypted SMS. iMessage is end-to-end encrypted between Apple devices, but its implementation is closed source and cloud-backup protection depends on account settings such as Advanced Data Protection. Both still depend on device security.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Signal actually private and secure?</h3>
            </summary>
            <div class="faq-body">
              Signal is genuinely private by mainstream-messenger standards: message content stays between participants, metadata collection is deliberately minimized, and the nonprofit has no advertising business built on user profiling. Important limits remain: registration requires a phone number, contacts who already know that number may identify you, and Signal cannot secure a compromised endpoint.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Signal really more secure than WhatsApp?</h3>
            </summary>
            <div class="faq-body">
              In one important way, yes: Signal is fully end-to-end encrypted by default on every chat, call, and group, and collects almost zero metadata. WhatsApp also uses the Signal Protocol for messages but does not hide who is messaging whom from Meta. For pure privacy Signal is the clear leader; for install-base WhatsApp wins.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Does Signal show my phone number to other people?</h3>
            </summary>
            <div class="faq-body">
              By default, yes — your phone number is the registration identity. Since 2024 you can create a Signal username that's separate from your number, but the underlying account is still bound to a real phone number. Mutuals who already have your number will see you.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Has Signal been independently audited?</h3>
            </summary>
            <div class="faq-body">
              Yes. Signal Protocol has been audited repeatedly by independent firms, and the Signal app code (both client and server) is open-source. Unlike WhatsApp and XChat, anyone can verify the implementation.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">How does Signal make money if it is free?</h3>
            </summary>
            <div class="faq-body">
              Signal is run by the nonprofit Signal Foundation, founded by Brian Acton (WhatsApp co-founder) and Moxie Marlinspike. It operates on donations, including from Acton personally, and from grants. There is no advertising and no tracking.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Can I use Signal without giving my phone number?</h3>
            </summary>
            <div class="faq-body">
              Not entirely — you need a phone number to register. You can use the Signal username (added in 2024) to keep your number private from contacts, but the underlying account still requires a number at signup.
            </div>
          </details>
        </div>
      </div>
    </section>

    <!-- ============== MORE REVIEWS ============== -->
    <section class="section related-wrap">
      <div class="container">
        <h2 class="section-title">More encrypted messenger reviews</h2>
        <div class="related-grid">
          {otherApps.map((other) => (
            <a href={`/reviews/${other.slug}/`} class="related-card">
              <AppIcon app={other} class="related-monogram" />
              <h3>{other.name} review</h3>
              <p class="text-secondary">{other.tagline}</p>
              <span class="related-score text-muted">{other.editorScore.toFixed(1)} / 10</span>
              <span class="related-arrow">→</span>
            </a>
          ))}
        </div>
        <p class="muted-note text-muted">
          Want to compare Signal with another messenger? See our
          <a href="/compare/signal-vs-threema/"> Signal vs Threema</a> and
          <a href="/compare/signal-vs-telegram/"> Signal vs Telegram</a> comparisons.
        </p>
      </div>
    </section>
  </article>
</Layout>

<style>
  /* ===== Hero ===== */
  .review-hero {
    text-align: center;
    padding: 6rem 0 3rem;
    border-bottom: 1px solid var(--border);
    background:
      radial-gradient(ellipse at top, rgba(37, 146, 233, 0.06), transparent 60%),
      var(--bg-primary);
  }

  .breadcrumb {
    font-size: 0.8125rem;
    margin-bottom: 2rem;
  }

  .breadcrumb a {
    color: var(--text-muted);
    text-decoration: none;
  }

  .crumb-sep {
    margin: 0 0.5rem;
    color: var(--text-muted);
  }

  .hero-app {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
    margin-bottom: 2.5rem;
  }

  .hero-monogram {
    width: 88px;
    height: 88px;
    border-radius: 22px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    font-weight: 800;
    color: white;
    box-shadow:
      0 0 0 1px rgba(255, 255, 255, 0.1),
      0 12px 32px rgba(0, 0, 0, 0.3);
    letter-spacing: -0.04em;
  }

  .hero-meta { text-align: center; }

  .eyebrow {
    display: inline-block;
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent-light);
    background: rgba(29, 155, 240, 0.08);
    border: 1px solid rgba(29, 155, 240, 0.25);
    border-radius: 9999px;
    padding: 0.3125rem 0.875rem;
    margin-bottom: 1rem;
  }

  .hero-meta h1 { max-width: 820px; margin: 0 auto 0.75rem; }
  .hero-tagline { font-size: 1.125rem; max-width: 600px; margin: 0 auto; }

  /* ===== Score panel ===== */
  .hero-score {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 2rem;
    align-items: center;
    max-width: 720px;
    margin: 0 auto;
    text-align: left;
    padding: 1.5rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
  }

  .score-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0 1.25rem;
    border-right: 1px solid var(--border);
    min-width: 110px;
  }

  .score-num {
    font-family: var(--font-mono);
    font-size: 3rem;
    font-weight: 800;
    line-height: 1;
    color: var(--accent-light);
    letter-spacing: -0.04em;
  }

  .score-out-of {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--text-muted);
    margin-top: 0.25rem;
  }

  .score-stars {
    font-size: 0.8125rem;
    margin-top: 0.5rem;
  }

  .score-summary p {
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--text-secondary);
    margin: 0 0 0.5rem 0;
  }

  .score-summary .small {
    font-size: 0.75rem;
  }

  /* ===== At a glance ===== */
  .section-title {
    text-align: center;
    margin-bottom: 2rem;
    font-size: clamp(1.5rem, 3vw, 2.25rem);
  }

  .glance-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 0.75rem;
    max-width: 920px;
    margin: 0 auto;
  }

  .glance-card {
    padding: 1rem 1.25rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .glance-label {
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .glance-value {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  /* ===== Prose ===== */
  .prose {
    max-width: 720px;
    margin: 0 auto;
    color: var(--text-secondary);
  }

  .prose h2 {
    color: var(--text-primary);
    font-size: 1.625rem;
    margin: 2.5rem 0 1.25rem;
    text-align: left;
    line-height: 1.25;
  }

  .prose h3 {
    color: var(--text-primary);
    font-size: 1.25rem;
    margin: 2rem 0 0.75rem;
  }

  .prose p {
    font-size: 1rem;
    line-height: 1.75;
    margin-bottom: 1.25rem;
  }

  .prose ul {
    margin: 0.5rem 0 1.5rem 1.5rem;
    list-style: disc;
  }

  .prose ul li {
    margin-bottom: 0.75rem;
    line-height: 1.7;
  }

  .prose strong { color: var(--text-primary); font-weight: 600; }
  .prose em { font-style: italic; color: var(--text-primary); }
  .prose a { color: var(--accent-light); text-decoration: none; border-bottom: 1px dotted rgba(77, 181, 245, 0.4); }
  .prose a:hover { border-bottom-color: var(--accent-light); }

  /* ===== Pros / Cons ===== */
  .pros-cons {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
    max-width: 920px;
    margin: 3rem auto 0;
  }

  .pros-cons h3 {
    font-size: 1.125rem;
    margin: 0 0 1rem;
  }

  .pros-cons ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }

  .pros-cons li {
    display: flex;
    align-items: flex-start;
    gap: 0.625rem;
    line-height: 1.55;
    font-size: 0.9375rem;
    color: var(--text-secondary);
  }

  .pros-cons li > span:first-child {
    flex-shrink: 0;
    width: 18px;
    height: 18px;
    border-radius: 4px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 0.875rem;
    font-weight: 700;
    margin-top: 0.125rem;
  }

  .card-pro { background: rgba(0, 186, 124, 0.04); border: 1px solid rgba(0, 186, 124, 0.2); border-radius: var(--radius-xl); padding: 1.5rem; }
  .card-pro h3 { color: var(--success); }
  .card-pro li > span:first-child { background: var(--success); color: #000; }

  .card-con { background: rgba(244, 33, 46, 0.04); border: 1px solid rgba(244, 33, 46, 0.2); border-radius: var(--radius-xl); padding: 1.5rem; }
  .card-con h3 { color: var(--danger); }
  .card-con li > span:first-child { background: var(--danger); color: #fff; }

  /* ===== Related grid ===== */
  .related-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1rem;
    max-width: 920px;
    margin: 0 auto 1.5rem;
  }

  .related-card {
    position: relative;
    padding: 1.5rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: inherit;
    transition: border-color 0.3s, transform 0.3s var(--ease-smooth);
  }

  .related-card:hover {
    border-color: var(--accent);
    transform: translateY(-2px);
  }

  .related-monogram {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    color: white;
    font-size: 0.9375rem;
    margin-bottom: 0.75rem;
  }

  .related-card h3 { font-size: 1rem; margin: 0 0 0.375rem; }
  .related-card p { font-size: 0.875rem; margin: 0; line-height: 1.5; }
  .related-score {
    display: block;
    margin-top: 0.625rem;
    font-size: 0.75rem;
    font-family: var(--font-mono);
  }
  .related-arrow {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    color: var(--text-muted);
    font-size: 1.125rem;
    transition: color 0.15s, transform 0.2s;
  }
  .related-card:hover .related-arrow { color: var(--accent-light); transform: translateX(2px); }

  .muted-note {
    text-align: center;
    font-size: 0.875rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--border);
    max-width: 600px;
    margin: 0 auto;
  }

  /* ===== FAQ list ===== */
  .faq-list {
    max-width: 720px;
    margin: 0 auto;
  }

  /* ===== Responsive ===== */
  @media (max-width: 720px) {
    .hero-score {
      grid-template-columns: 1fr;
      gap: 1.25rem;
      padding: 1.25rem;
    }
    .score-card {
      border-right: none;
      border-bottom: 1px solid var(--border);
      padding: 0 0 1.25rem;
      min-width: 0;
    }
    .score-summary p { text-align: left; }
    .pros-cons {
      grid-template-columns: 1fr;
    }
    .review-hero { padding-top: 4rem; padding-bottom: 2.5rem; }
    .hero-monogram { width: 72px; height: 72px; font-size: 1.75rem; }
    .hero-meta h1 { max-width: 100%; }
    .hero-tagline { font-size: 1rem; max-width: 100%; }
    .glance-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 420px) {
    .glance-grid { grid-template-columns: 1fr; }
  }
</style>
