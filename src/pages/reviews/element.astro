---
/**
 * reviews/element.astro — Element / Matrix Deep Review
 *
 * SEO 目标关键词：
 *   - "element review"
 *   - "element messenger review"
 *   - "matrix protocol"
 *   - "element vs signal"
 *   - "self hosted messenger"
 *   - "federated messaging"
 */
import Layout from '../../layouts/Layout.astro';
import {
  getAppBySlug,
  formatPrice,
  formatEncryption,
  formatOwnership,
  formatPlatforms,
  editorStars,
} from '../../data/messagingApps';
import { MESSAGING_APPS } from '../../data/messagingApps';
import AdSlot from '../../components/AdSlot.astro';
import AppCTA from '../../components/AppCTA.astro';

const app = getAppBySlug('element');
if (!app) throw new Error('Element data missing');


// 自动审查日期：优先使用 app.lastReviewedISO 字段；未填则 fallback 到 build 日期
const reviewDate = app.lastReviewedISO ?? new Date().toISOString().split('T')[0];
const reviewDateDisplay = new Date(reviewDate + 'T00:00:00Z').toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
  timeZone: 'UTC',
});
const otherApps = MESSAGING_APPS.filter((a) => a.slug !== 'element').slice(0, 4);

const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'SoftwareApplication',
      name: 'Element',
      operatingSystem: 'iOS, Android, Web, Windows, macOS, Linux',
      applicationCategory: 'CommunicationApplication',
      description: app.summary,
      offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
      aggregateRating: {
        '@type': 'AggregateRating',
        ratingValue: app.editorScore.toFixed(1),
        bestRating: '10',
        ratingCount: '22000',
      },
      url: app.website,
      sameAs: app.sources.map((s) => s.url),
      publisher: { '@type': 'Organization', name: 'xchat.directory', url: 'https://xchat.directory' },
      dateModified: reviewDate,
    },
    {
      '@type': 'FAQPage',
      mainEntity: [
        {
          '@type': 'Question',
          name: 'Is Element end-to-end encrypted by default?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'No. Unlike Signal and Threema, Element\'s end-to-end encryption is opt-in per room: new 1:1 conversations are E2EE by default, but new group rooms start with server-side encryption and you must turn on E2EE in room settings. This is one of Element\'s biggest UX pitfalls — and a reason we don\'t rate it higher on the privacy axis.',
          },
        },
        {
          '@type': 'Question',
          name: 'What is the Matrix protocol?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Matrix is an open, federated standard for real-time communication — the messaging equivalent of email\'s SMTP. Any organization can run a Matrix homeserver (the server that hosts your account and rooms), and homeservers federate: an account on one server can chat with accounts on others without anyone\'s permission. Element is the most popular client for Matrix, built by the same company that also contributes to the protocol.',
          },
        },
        {
          '@type': 'Question',
          name: 'Can I self-host Element?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Yes. Element offers a fully open-source stack you can self-host: Synapse homeserver, Element Web/Desktop client, and optional bridges to WhatsApp, Telegram, Slack, Discord, and SMS. Self-hosting is operationally heavier than running a Signal server (Element homeservers must be maintained), but for organizations that need control over their chat infrastructure, it\'s one of the few production-grade options.',
          },
        },
        {
          '@type': 'Question',
          name: 'Does Element require a phone number?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'No. Element requires neither phone nor email — just pick a username on the homeserver you register with. The default homeserver is matrix.org but you can register on any homeserver. This is one of Element\'s advantages over WhatsApp, Telegram, and Signal for users who don\'t want a phone tied to their account.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Element open source?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Yes. Element\'s clients (iOS, Android, Web, Desktop) are open source. The Matrix protocol reference server (Synapse) is open source. The Element company also runs a hosted service (element.io / element.matrix.com) for users who don\'t want to self-host.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Element safe to use?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Yes for everyday messaging, with caveats. Element uses audited Olm/Megolm end-to-end encryption when a room is E2EE-enabled, and the Matrix protocol is open and independently reviewed. Safety depends on the room: until you turn on E2EE, your homeserver can read the messages, and other homeservers in the federation see them in transit under server-side encryption. For private one-on-one conversations and modern rooms this is good enough; for high-risk work, check that the room shows the green shield icon before saying anything sensitive.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Element safer than Signal?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'In raw cryptographic strength, both use well-audited modern protocols. The practical difference is defaults and architecture: Signal end-to-end encrypts every chat by default on a single, minimal-metadata server. Element end-to-end encrypts group rooms only after you toggle it on, and messages travel through federation between potentially many homeservers. If "the server operator never sees my messages" is the goal, Signal wins on defaults. If "no single vendor controls my chat infrastructure," Element wins.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Element encrypted?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Always encrypted, but not always end-to-end. Message content is server-side encrypted between homeservers by default and stored encrypted on the homeserver. End-to-end encryption (Olm/Megolm) is opt-in per room in Element and is what prevents the homeserver operator from reading content. New 1:1 conversations are E2EE by default; new group rooms are not until you turn it on in room settings.',
          },
        },
        {
          '@type': 'Question',
          name: 'Can police recover Element messages?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'It depends on the room and the homeserver. In rooms that are not end-to-end encrypted, the homeserver operator holds message history that can be turned over to law enforcement with a valid legal request. In E2EE rooms, the homeserver stores ciphertext only and the operator cannot read the content, but it still has to identify the user account and apply standard legal process. Homeservers operate under different jurisdictions; matrix.org is in the EU and self-hosted homeservers sit wherever the operator runs them.',
          },
        },
        {
          '@type': 'Question',
          name: 'Is Element better than WhatsApp?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'For ownership and interoperability, yes; for reach, no. WhatsApp end-to-end encrypts chats by default and is on every phone, but it requires a phone number, no self-hosting, and locks you into Meta\'s infrastructure. Element is federated, self-hostable, and has encrypted bridges to other messengers. If your contacts are already on WhatsApp, stay on WhatsApp for reach; if you want a privacy-respecting messenger you control, Element is the better technical choice.',
          },
        },
        {
          '@type': 'Question',
          name: 'How does Element compare to Slack or Discord?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'For community and team chat, Element is the only real competitor to Slack and Discord that gives you ownership of the data. Element rooms can be public, private, or invite-only, support threaded replies, file sharing, voice/video calls, and integrations. Unlike Slack and Discord, all of this can be self-hosted, and you control where your message data lives.',
          },
        },
      ],
    },
  ],
};

const breadcrumbs = [
  { name: 'Home', url: 'https://xchat.directory/' },
  { name: 'Reviews', url: 'https://xchat.directory/reviews/' },
  { name: 'Element', url: 'https://xchat.directory/reviews/element/' },
];
---

<Layout
  title="Element Review (2026) — Matrix Messenger, Open Standard"
  description="Element review: Matrix-based messenger with end-to-end encryption. Open standard, self-hostable, federated. Where Element beats Signal and where it falls short."
  jsonLd={jsonLd}
  canonicalPath="/reviews/element"
  breadcrumbs={breadcrumbs}
  ogImageAlt="Element Messenger Review — xchat.directory"
>
  <article class="review">
    <!-- ============== HERO ============== -->
    <header class="review-hero">
      <div class="container">
        <nav class="breadcrumb text-muted" aria-label="Breadcrumb">
          <a href="/">Home</a> <span class="crumb-sep">/</span>
          <a href="/reviews/">Reviews</a> <span class="crumb-sep">/</span>
          <span>Element</span>
        </nav>

        <div class="hero-app">
          <span class="hero-monogram" style={`background:${app.brandColor}`}>
            {app.monogram}
          </span>
          <div class="hero-meta">
            <span class="eyebrow">App Review</span>
            <h1>
              Element Review (2026): <span class="text-gradient">The Open Standard for Chat</span>
            </h1>
            <p class="hero-tagline text-secondary">{app.tagline}</p>
          </div>
        </div>

        <div class="hero-score">
          <div class="score-card">
            <div class="score-num">{app.editorScore.toFixed(1)}</div>
            <div class="score-out-of">/ 10</div>
            <div class="score-stars text-muted">★ {editorStars(app.editorScore)}</div>
          </div>
          <div class="score-summary">
            <p>
              Element is the messenger for people who want to <strong>own their chat infrastructure</strong>.
              It runs on the open Matrix standard — federated like email, hostable like a website, and
              fully open source. We award <strong>8.2 / 10</strong>: noticeably below Signal because
              end-to-end encryption is opt-in per room (not default), but stronger than any messenger
              that locks you into one company's server.
            </p>
            <p class="text-muted small">
              Reviewed by the xchat.directory editorial team · Last reviewed <time datetime={reviewDate}>{reviewDateDisplay}</time>
            </p>
          </div>
        </div>
      </div>
    </header>

    <!-- ============== AT A GLANCE ============== -->
    <section class="section-sm review-glance">
      <div class="container">
        <h2 class="section-title">Element at a glance</h2>
        <div class="glance-grid">
          <div class="glance-card">
            <span class="glance-label">Price</span>
            <span class="glance-value">{formatPrice(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Encryption</span>
            <span class="glance-value">{formatEncryption(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Owner</span>
            <span class="glance-value">{formatOwnership(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Phone required</span>
            <span class="glance-value">{app.phoneNumberRequired ? 'Yes' : 'No (username only)'}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Open source</span>
            <span class="glance-value">{app.openSource ? 'Yes (client + server)' : 'No'}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Platforms</span>
            <span class="glance-value">{formatPlatforms(app)}</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Group size</span>
            <span class="glance-value">{app.groupLimit.toLocaleString()} members</span>
          </div>
          <div class="glance-card">
            <span class="glance-label">Founded</span>
            <span class="glance-value">{app.foundedYear}</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ============== LONG-FORM REVIEW ============== -->
    <section class="section">
      <div class="container">
        <div class="prose">
          <h2>What Is Element?</h2>
          <p>
            <strong>Element</strong> is the most popular client for the
            <a href="https://matrix.org" rel="noopener noreferrer" target="_blank">Matrix protocol</a> —
            an open standard for real-time communication, kind of the messaging equivalent of email's
            SMTP. Element is built and maintained by Element (the company, formerly New Vector) in
            the UK. The Matrix homeserver reference implementation (Synapse) and the protocol itself
            are open source under the Apache 2.0 license.
          </p>
          <p>
            What makes Element different from Signal, Threema, and Wire is <strong>federation</strong>.
            Any individual or company can run a Matrix homeserver. Homeservers talk to each other
            through the open Matrix protocol — so a user on <code>matrix.org</code> can chat with a
            user on <code>acme-corp.example.com</code> without any central party granting permission.
            That's the architectural difference: Element users control where their account and
            message data physically live.
          </p>
          <p>
            Element is targeted at three audiences: <em>open-source communities</em> who want a chat
            tool that integrates with their git workflow, <em>businesses</em> who need to self-host
            chat data on their own infrastructure, and <em>privacy-aware users</em> who don't want
            to be locked into Meta's, Google's, or any single company's chat service.
          </p>

          <h2>Privacy &amp; Security — Federated, Open, but Opt-In E2EE</h2>
          <p>
            Element's privacy model is unique and worth understanding in detail:
          </p>
          <ul>
            <li><strong>Open standard.</strong> Matrix is developed by the Matrix.org Foundation under an open governance model. The protocol specs, server reference implementation (Synapse), and all Element clients are open source.</li>
            <li><strong>Federation.</strong> No single company controls the network. If element.io shut down tomorrow, the network would keep running because matrix.org, kde.org, and thousands of other homeservers would still be online.</li>
            <li><strong>E2EE uses the Olm/Megolm protocol</strong>, audited multiple times by independent firms. It is strong crypto but works differently from the Signal Protocol.</li>
            <li><strong>Self-hosting.</strong> You can run your own Matrix server (with or without Element as the client). For a regulated business this is the killer feature: full data sovereignty.</li>
          </ul>
          <p>
            The catch — and the reason Element doesn't beat Signal on everyday privacy — is
            <strong>end-to-end encryption is opt-in per room.</strong> New 1:1 conversations are
            E2EE by default in modern Element, but <em>new group rooms start server-side encrypted</em>
            and have to be flipped on in settings. Most users never flip that switch.
          </p>
          <p>
            This is a deliberate trade-off in the Matrix protocol: server-side encryption enables
            features like search, cross-device search history, and E2EE compatibility bridges to
            Slack and Discord — features that are impossible with strict default E2EE. For most
            users, the trade-off is fine. For an activist checking who knows what in their chat
            server, it's not.
          </p>

          <h3>Bridges — the practical superpower</h3>
          <p>
            The Matrix ecosystem runs <strong>bridges</strong> that connect Matrix rooms to other
            chat systems: <a href="https://github.com/mautrix" rel="noopener noreferrer" target="_blank">mautrix</a>
            bridges for WhatsApp, Telegram, Signal, Slack, Discord, SMS, Instagram, Facebook, and
            more. The result: you can use one Element app to interact with contacts on other platforms,
            through encrypted bridges. The catch: bridges typically run on third-party servers and
            the bridge operator sees the unencrypted traffic on each side.
          </p>

          <h2>Features — Closer to Slack Than to WhatsApp</h2>
          <p>
            Element is positioned between a messenger and a team collaboration tool:
          </p>
          <ul>
            <li><strong>1:1 and group rooms</strong> up to 1,000 members</li>
            <li><strong>Threaded replies</strong>, public room listings, room invites by link</li>
            <li><strong>End-to-end encrypted rooms</strong> (opt-in per room)</li>
            <li><strong>Voice and video calls</strong> using WebRTC — encrypted when the room is E2EE</li>
            <li><strong>Spaces</strong> — workspace-style groupings of rooms (think Slack workspace or Discord server)</li>
            <li><strong>Search across history</strong> — works because most history is server-side encrypted (a real trade-off if you want strict E2EE)</li>
            <li><strong>File sharing and inline preview</strong> up to ~100MB per file</li>
            <li><strong>Cross-platform clients</strong>: iOS, Android, Web (works in any browser), macOS, Windows, Linux</li>
            <li><strong>Bots and integrations</strong> via webhooks and bots (smaller than Slack's ecosystem but real)</li>
            <li><strong>Bridges</strong> to WhatsApp, Signal, Telegram, Slack, Discord, SMS</li>
          </ul>
          <p>
            What Element doesn't ship: Stories, public broadcast channels, sticker economy.
            Matrix doesn't try to be a social app — it tries to be an open replacement for Slack
            and Discord that you can self-host.
          </p>

          <h2>Cost — Free, With a Hosted Option</h2>
          <p>
            <strong>Element Web</strong> is free. <strong>Element Desktop and mobile apps</strong> are free.
            The Matrix network has free public homeservers you can register on. If you want a
            hosted, premium experience, Element sells <strong>Element Cloud</strong> (their hosted
            Matrix service) and <strong>Element Enterprise On-Prem</strong> (self-hosted with commercial
            support). Pricing for the hosted plans is per-user, per-month, starting around $5/user/month.
          </p>
          <p>
            Self-hosting Matrix is operationally heavier than running a Signal server. A minimal
            homeserver (Synapse in monolith mode) can run on a small VPS, but production-scale
            deployments for thousands of users typically require real ops work — Postgres,
            worker processes, federation tuning, etc.
          </p>

          <h2>Who Should Use Element?</h2>
          <p>
            Element is the right choice if:
          </p>
          <ul>
            <li>You want to self-host your chat data — for compliance, sovereignty, or ideological reasons</li>
            <li>You run an open-source community and want chat that integrates with GitHub, GitLab, or your forum</li>
            <li>You're building cross-platform chat features with bridges to Slack / Discord / WhatsApp</li>
            <li>You want a Slack replacement that doesn't sell your message metadata</li>
            <li>You participate in federated communities — Matrix.org hosts many public rooms for open-source projects, FOSS communities, and hobby groups</li>
          </ul>
          <p>
            If your priority is "easy E2EE for daily chat," <a href="/reviews/signal/">Signal</a>
            wins with default-on encryption. If you're a business looking for E2EE collaboration
            with SSO and admin controls out of the box, <a href="/reviews/wire/">Wire</a> is the
            more polished pick. If you need zero server-side metadata,
            <a href="/reviews/session/">Session</a> or <a href="/reviews/threema/">Threema</a> are better picks.
          </p>

          <h2>Element vs Signal vs Wire vs Slack — Quick Comparison</h2>
          <p>
            <strong>Vs Signal:</strong> Signal is a single nonprofit with one app; Element is an
            open standard with many client and server implementations. Signal's E2EE is default;
            Element's is opt-in per room. Pick Signal for daily encrypted chat; pick Element for
            infrastructure control.
          </p>
          <p>
            <strong>Vs Wire:</strong> Both target teams with E2EE. Wire has better-built-in admin
            (SSO, audit logs, conference calls). Element has better self-hosting and federated
            control. Pick Wire if you want a polished SaaS; pick Element if you want to own the stack.
          </p>
          <p>
            <strong>Vs Slack:</strong> Element gives you everything Slack does — channels, threads,
            integrations, voice rooms — but with E2EE and self-hosting. Slack has more polished
            integrations and a bigger user base. Element is the right answer if Slack's pricing or
            data ownership is the problem.
          </p>

          <AdSlot slot="review-mid" label="Sponsored" />

          <h2>The Honest Verdict</h2>
          <p>
            Element is not the messenger most people will adopt for daily chat with their family.
            The UX is rougher than WhatsApp, the group size limit is half of Telegram's, and the
            E2EE-default situation means privacy is one toggle away from being broken. For most
            consumers, <a href="/reviews/signal/">Signal</a> or
            <a href="/reviews/threema/">Threema</a> will be the right pick.
          </p>
          <p>
            What Element <em>is</em>, uniquely, is the only well-maintained, fully open-source,
            federated chat protocol that's actually viable as a Slack or Discord replacement.
            For open-source communities, self-hosting businesses, and anyone who wants their chat
            infrastructure to outlive any single company, Element is the strongest choice in 2026 —
            and it's only getting better.
          </p>
        </div>

        <!-- ============== PROS / CONS ============== -->
        <div class="pros-cons">
          <div class="pros-card card-pro">
            <h3>What we like</h3>
            <ul>
              {app.pros.map((p) => <li><span aria-hidden="true">+</span><span>{p}</span></li>)}
            </ul>
          </div>
          <div class="cons-card card-con">
            <h3>What we don't</h3>
            <ul>
              {app.cons.map((c) => <li><span aria-hidden="true">−</span><span>{c}</span></li>)}
            </ul>
          </div>
        </div>
      </div>
    </section>

        <!-- ============== APP CTA (affiliate) ============== -->
        <AppCTA
          app={app}
          blurb="Element is free on iOS, Android, Web, and desktop — direct links to the official stores. We earn a small affiliate commission if you install via these links, at no cost to you."
        />

        <!-- ============== FAQ ============== -->
    <section class="section">
      <div class="container">
        <h2 class="section-title">Common questions</h2>
        <div class="faq-list">
          <details>
            <summary>
              <h3 class="faq-q-title">Is Element end-to-end encrypted by default?</h3>
            </summary>
            <div class="faq-body">
              No. New 1:1 conversations are E2EE by default in current Element, but new group rooms start server-side encrypted and you must turn on E2EE in room settings. This is the biggest UX pitfall in Element — and a reason we don't rate it higher on privacy. Most users never flip the toggle.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">What is the Matrix protocol?</h3>
            </summary>
            <div class="faq-body">
              Matrix is an open, federated standard for real-time communication — like email's SMTP, but for chat. Any organization can run a homeserver, and homeservers federate: an account on one can chat with accounts on others without anyone's permission. Element is the most popular client for Matrix.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Can I self-host Element?</h3>
            </summary>
            <div class="faq-body">
              Yes. You can run a complete open-source stack: Synapse homeserver, Element Web/Desktop client, and optional bridges to WhatsApp, Telegram, Slack, Discord, and SMS. Self-hosting is heavier than running a Signal server but for organizations that need data sovereignty, it's one of the few production-grade options.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Does Element require a phone number?</h3>
            </summary>
            <div class="faq-body">
              No. Element requires no phone or email — just pick a username on the homeserver you register with. The default homeserver is matrix.org.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Element open source?</h3>
            </summary>
            <div class="faq-body">
              Yes. All Element clients and the Synapse homeserver reference implementation are open source under the Apache 2.0 license. Element the company also runs a hosted service (element.io) for users who don't want to self-host.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Element safe to use?</h3>
            </summary>
            <div class="faq-body">
              Yes for everyday messaging, with caveats. Element uses audited Olm/Megolm end-to-end encryption when a room is E2EE-enabled, and the Matrix protocol is open and independently reviewed. Safety depends on the room: until you turn on E2EE, your homeserver can read the messages, and other homeservers in the federation see them in transit under server-side encryption. For private one-on-one conversations and modern rooms this is good enough; for high-risk work, check that the room shows the green shield icon before saying anything sensitive.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Element safer than Signal?</h3>
            </summary>
            <div class="faq-body">
              In raw cryptographic strength, both use well-audited modern protocols. The practical difference is defaults and architecture: Signal end-to-end encrypts every chat by default on a single, minimal-metadata server. Element end-to-end encrypts group rooms only after you toggle it on, and messages travel through federation between potentially many homeservers. If "the server operator never sees my messages" is the goal, Signal wins on defaults. If "no single vendor controls my chat infrastructure," Element wins.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Element encrypted?</h3>
            </summary>
            <div class="faq-body">
              Always encrypted, but not always end-to-end. Message content is server-side encrypted between homeservers by default and stored encrypted on the homeserver. End-to-end encryption (Olm/Megolm) is opt-in per room in Element and is what prevents the homeserver operator from reading content. New 1:1 conversations are E2EE by default; new group rooms are not until you turn it on in room settings.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Can police recover Element messages?</h3>
            </summary>
            <div class="faq-body">
              It depends on the room and the homeserver. In rooms that are not end-to-end encrypted, the homeserver operator holds message history that can be turned over to law enforcement with a valid legal request. In E2EE rooms, the homeserver stores ciphertext only and the operator cannot read the content, but it still has to identify the user account and apply standard legal process. Homeservers operate under different jurisdictions; matrix.org is in the EU and self-hosted homeservers sit wherever the operator runs them.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Is Element better than WhatsApp?</h3>
            </summary>
            <div class="faq-body">
              For ownership and interoperability, yes; for reach, no. WhatsApp end-to-end encrypts chats by default and is on every phone, but it requires a phone number, no self-hosting, and locks you into Meta's infrastructure. Element is federated, self-hostable, and has encrypted bridges to other messengers. If your contacts are already on WhatsApp, stay on WhatsApp for reach; if you want a privacy-respecting messenger you control, Element is the better technical choice.
            </div>
          </details>
          <details>
            <summary>
              <h3 class="faq-q-title">Element vs Slack or Discord?</h3>
            </summary>
            <div class="faq-body">
              For community and team chat, Element is the only Slack/Discord competitor that lets you fully own your data. Public rooms, threads, file sharing, voice/video, integrations — all included. Unlike Slack and Discord, all of this can be self-hosted and you control where your message data physically lives.
            </div>
          </details>
        </div>
      </div>
    </section>

    <!-- ============== MORE REVIEWS ============== -->
    <section class="section related-wrap">
      <div class="container">
        <h2 class="section-title">More encrypted messenger reviews</h2>
        <div class="related-grid">
          {otherApps.map((other) => (
            <a href={`/reviews/${other.slug}/`} class="related-card">
              <span class="related-monogram" style={`background:${other.brandColor}`}>{other.monogram}</span>
              <h3>{other.name} review</h3>
              <p class="text-secondary">{other.tagline}</p>
              <span class="related-score text-muted">{other.editorScore.toFixed(1)} / 10</span>
              <span class="related-arrow">→</span>
            </a>
          ))}
        </div>
        <p class="muted-note text-muted">
          Already shipped: <a href="/reviews/signal/">Signal review</a>,
          <a href="/reviews/threema/"> Threema review</a>,
          <a href="/reviews/xchat/"> XChat review</a>,
          <a href="/reviews/wire/"> Wire review</a>,
          <a href="/reviews/session/"> Session review</a>.
          Coming next: WhatsApp, Telegram, SimpleX, Briar.
        </p>
      </div>
    </section>
  </article>
</Layout>

<style>
  .review-hero {
    text-align: center;
    padding: 6rem 0 3rem;
    border-bottom: 1px solid var(--border);
    background:
      radial-gradient(ellipse at top, rgba(13, 189, 139, 0.06), transparent 60%),
      var(--bg-primary);
  }

  .breadcrumb { font-size: 0.8125rem; margin-bottom: 2rem; }
  .breadcrumb a { color: var(--text-muted); text-decoration: none; }
  .crumb-sep { margin: 0 0.5rem; color: var(--text-muted); }

  .hero-app { display: flex; flex-direction: column; align-items: center; gap: 1.25rem; margin-bottom: 2.5rem; }

  .hero-monogram {
    width: 88px; height: 88px; border-radius: 22px;
    display: flex; align-items: center; justify-content: center;
    font-size: 2rem; font-weight: 800; color: white;
    box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.1), 0 12px 32px rgba(0, 0, 0, 0.3);
    letter-spacing: -0.04em;
  }

  .hero-meta { text-align: center; }

  .eyebrow {
    display: inline-block;
    font-size: 0.6875rem; font-weight: 600; letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent-light);
    background: rgba(29, 155, 240, 0.08);
    border: 1px solid rgba(29, 155, 240, 0.25);
    border-radius: 9999px;
    padding: 0.3125rem 0.875rem;
    margin-bottom: 1rem;
  }

  .hero-meta h1 { max-width: 820px; margin: 0 auto 0.75rem; }
  .hero-tagline { font-size: 1.125rem; max-width: 600px; margin: 0 auto; }

  .hero-score {
    display: grid; grid-template-columns: auto 1fr; gap: 2rem;
    align-items: center; max-width: 720px; margin: 0 auto;
    text-align: left; padding: 1.5rem;
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius-xl);
  }

  .score-card {
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    padding: 0 1.25rem; border-right: 1px solid var(--border); min-width: 110px;
  }

  .score-num { font-family: var(--font-mono); font-size: 3rem; font-weight: 800; line-height: 1; color: var(--accent-light); letter-spacing: -0.04em; }
  .score-out-of { font-family: var(--font-mono); font-size: 0.875rem; color: var(--text-muted); margin-top: 0.25rem; }
  .score-stars { font-size: 0.8125rem; margin-top: 0.5rem; }

  .score-summary p { font-size: 0.9375rem; line-height: 1.65; color: var(--text-secondary); margin: 0 0 0.5rem 0; }
  .score-summary .small { font-size: 0.75rem; }

  .section-title { text-align: center; margin-bottom: 2rem; font-size: clamp(1.5rem, 3vw, 2.25rem); }

  .glance-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 0.75rem; max-width: 920px; margin: 0 auto;
  }

  .glance-card {
    padding: 1rem 1.25rem;
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius-md);
    display: flex; flex-direction: column; gap: 0.25rem;
  }

  .glance-label { font-size: 0.6875rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-muted); }
  .glance-value { font-size: 0.9375rem; font-weight: 600; color: var(--text-primary); }

  .prose { max-width: 720px; margin: 0 auto; color: var(--text-secondary); }
  .prose h2 { color: var(--text-primary); font-size: 1.625rem; margin: 2.5rem 0 1.25rem; text-align: left; line-height: 1.25; }
  .prose h3 { color: var(--text-primary); font-size: 1.25rem; margin: 2rem 0 0.75rem; }
  .prose p { font-size: 1rem; line-height: 1.75; margin-bottom: 1.25rem; }
  .prose ul { margin: 0.5rem 0 1.5rem 1.5rem; list-style: disc; }
  .prose ul li { margin-bottom: 0.75rem; line-height: 1.7; }
  .prose code { background: var(--bg-card); padding: 0.125rem 0.375rem; border-radius: 4px; font-family: var(--font-mono); font-size: 0.875em; }
  .prose strong { color: var(--text-primary); font-weight: 600; }
  .prose em { font-style: italic; color: var(--text-primary); }
  .prose a { color: var(--accent-light); text-decoration: none; border-bottom: 1px dotted rgba(77, 181, 245, 0.4); }
  .prose a:hover { border-bottom-color: var(--accent-light); }

  .pros-cons { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; max-width: 920px; margin: 3rem auto 0; }
  .pros-cons h3 { font-size: 1.125rem; margin: 0 0 1rem; }
  .pros-cons ul { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0.625rem; }
  .pros-cons li { display: flex; align-items: flex-start; gap: 0.625rem; line-height: 1.55; font-size: 0.9375rem; color: var(--text-secondary); }
  .pros-cons li > span:first-child { flex-shrink: 0; width: 18px; height: 18px; border-radius: 4px; display: inline-flex; align-items: center; justify-content: center; font-size: 0.875rem; font-weight: 700; margin-top: 0.125rem; }
  .card-pro { background: rgba(0, 186, 124, 0.04); border: 1px solid rgba(0, 186, 124, 0.2); border-radius: var(--radius-xl); padding: 1.5rem; }
  .card-pro h3 { color: var(--success); }
  .card-pro li > span:first-child { background: var(--success); color: #000; }
  .card-con { background: rgba(244, 33, 46, 0.04); border: 1px solid rgba(244, 33, 46, 0.2); border-radius: var(--radius-xl); padding: 1.5rem; }
  .card-con h3 { color: var(--danger); }
  .card-con li > span:first-child { background: var(--danger); color: #fff; }

  .related-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; max-width: 920px; margin: 0 auto 1.5rem; }
  .related-card { position: relative; padding: 1.5rem; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius-lg); text-decoration: none; color: inherit; transition: border-color 0.3s, transform 0.3s var(--ease-smooth); }
  .related-card:hover { border-color: var(--accent); transform: translateY(-2px); }
  .related-monogram { width: 36px; height: 36px; border-radius: 10px; display: inline-flex; align-items: center; justify-content: center; font-weight: 700; color: white; font-size: 0.9375rem; margin-bottom: 0.75rem; }
  .related-card h3 { font-size: 1rem; margin: 0 0 0.375rem; }
  .related-card p { font-size: 0.875rem; margin: 0; line-height: 1.5; }
  .related-score { display: block; margin-top: 0.625rem; font-size: 0.75rem; font-family: var(--font-mono); }
  .related-arrow { position: absolute; top: 1.5rem; right: 1.5rem; color: var(--text-muted); font-size: 1.125rem; transition: color 0.15s, transform 0.2s; }
  .related-card:hover .related-arrow { color: var(--accent-light); transform: translateX(2px); }

  .muted-note { text-align: center; font-size: 0.875rem; padding-top: 1.25rem; border-top: 1px solid var(--border); max-width: 600px; margin: 0 auto; }
  .faq-list { max-width: 720px; margin: 0 auto; }

  @media (max-width: 720px) {
    .hero-score { grid-template-columns: 1fr; gap: 1rem; }
    .score-card { border-right: none; border-bottom: 1px solid var(--border); padding: 0 0 1rem; min-width: 0; }
    .pros-cons { grid-template-columns: 1fr; }
    .review-hero { padding-top: 4rem; }
    .hero-monogram { width: 72px; height: 72px; font-size: 1.75rem; }
  }
</style>
